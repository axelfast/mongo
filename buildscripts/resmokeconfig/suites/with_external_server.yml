test_kind: js_test

# Use this suite to connect to a running mongerd and specify resmoke.py options
# '--shellConnPort' or '--shellConnString'.
executor:
  config:
    shell_options:
      readMode: commands
