test_kind: js_test

selector:
  roots: []

executor:
  config:
    shell_options:
      readMode: commands
  fixture:
    class: MongoDFixture
    mongerd_options:
      set_parameters:
        enableTestCommands: 1
